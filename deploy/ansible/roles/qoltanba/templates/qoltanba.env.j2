# Managed by Ansible (role: qoltanba). Non-secret settings only.
# Secrets go via systemd LoadCredential + QOLTANBA_<KEY>_FILE (see the unit).
{% for key, value in qoltanba_env.items() %}
{{ key }}={{ value }}
{% endfor %}

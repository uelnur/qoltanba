# Code generated by tools/openapigen; DO NOT EDIT.
# Regenerate with `make openapi`.
components:
  responses:
    Error:
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
      description: Hard failure envelope (friendly message from the error catalog)
  schemas:
    CertInfoRequest:
      properties:
        buildChain:
          type: boolean
        cert:
          contentEncoding: base64
          type: string
        claims:
          type: boolean
        encoding:
          enum:
          - pem
          - der
          - base64
          type: string
        key:
          $ref: '#/components/schemas/KeySpec'
        method:
          enum:
          - ocsp
          - crl
          type: string
        trustedCerts:
          items:
            $ref: '#/components/schemas/TrustedCert'
          type: array
        validate:
          type: boolean
      type: object
    CertInfoResponse:
      properties:
        certificate:
          $ref: '#/components/schemas/Certificate'
        chain:
          items:
            $ref: '#/components/schemas/Certificate'
          type: array
        claims:
          $ref: '#/components/schemas/Claims'
        libError:
          $ref: '#/components/schemas/LibError'
        warnings:
          items:
            $ref: '#/components/schemas/Warning'
          type: array
      required:
      - certificate
      type: object
    Certificate:
      properties:
        authorityKeyId:
          type: string
        caIssuerUrls:
          items:
            type: string
          type: array
        crlUrls:
          items:
            type: string
          type: array
        extendedKeyUsage:
          items:
            type: string
          type: array
        isCa:
          type: boolean
        issuer:
          $ref: '#/components/schemas/Subject'
        keyAlgorithm:
          type: string
        keyUsage:
          items:
            type: string
          type: array
        keyUsageKind:
          type: string
        notAfter:
          format: date-time
          type: string
        notBefore:
          format: date-time
          type: string
        ocspUrls:
          items:
            type: string
          type: array
        ownerType:
          enum:
          - INDIVIDUAL
          - LEGAL_PERSON
          - INFOSYSTEM
          - UNKNOWN
          type: string
        pem:
          contentEncoding: base64
          type: string
        policyOids:
          items:
            type: string
          type: array
        publicKey:
          contentEncoding: base64
          type: string
        roles:
          items:
            type: string
          type: array
        serialNumber:
          type: string
        signatureAlgorithm:
          type: string
        signatureAlgorithmOid:
          type: string
        subject:
          $ref: '#/components/schemas/Subject'
        subjectKeyId:
          type: string
      required:
      - subject
      - issuer
      type: object
    Claims:
      properties:
        bin:
          type: string
        email:
          type: string
        family_name:
          type: string
        gender:
          enum:
          - male
          - female
          type: string
        given_name:
          type: string
        iin:
          type: string
        name:
          type: string
        organization:
          type: string
        owner_type:
          enum:
          - INDIVIDUAL
          - LEGAL_PERSON
          - INFOSYSTEM
          - UNKNOWN
          type: string
        roles:
          items:
            type: string
          type: array
        sub:
          type: string
      type: object
    ErrorEnvelope:
      properties:
        error:
          properties:
            action:
              type: string
            code:
              type: string
            kind:
              type: string
            message:
              type: string
          required:
          - kind
          - message
          type: object
      required:
      - error
      type: object
    ExtractRequest:
      properties:
        data:
          contentEncoding: base64
          type: string
        format:
          enum:
          - cms
          - xml
          - wsse
          type: string
        signature:
          contentEncoding: base64
          type: string
      required:
      - format
      - signature
      type: object
    ExtractResponse:
      properties:
        content:
          contentEncoding: base64
          type: string
        detached:
          type: boolean
        libError:
          $ref: '#/components/schemas/LibError'
      required:
      - detached
      type: object
    InlineKey:
      properties:
        alias:
          type: string
        p12:
          contentEncoding: base64
          type: string
        password:
          type: string
      required:
      - p12
      - password
      type: object
    KeySpec:
      properties:
        inline:
          $ref: '#/components/schemas/InlineKey'
        keyId:
          type: string
        path:
          $ref: '#/components/schemas/PathKey'
        token:
          $ref: '#/components/schemas/TokenKey'
      type: object
    LibError:
      properties:
        action:
          type: string
        code:
          type: string
        key:
          type: string
        message:
          type: string
        text:
          type: string
      required:
      - code
      type: object
    PathKey:
      properties:
        alias:
          type: string
        password:
          type: string
        path:
          type: string
      required:
      - path
      type: object
    RevocationStatus:
      properties:
        checkedAt:
          format: date-time
          type: string
        libError:
          $ref: '#/components/schemas/LibError'
        method:
          enum:
          - ocsp
          - crl
          type: string
        nextUpdate:
          format: date-time
          type: string
        producedAt:
          format: date-time
          type: string
        reason:
          type: string
        revocationTime:
          format: date-time
          type: string
        revoked:
          type: boolean
        thisUpdate:
          format: date-time
          type: string
      required:
      - revoked
      type: object
    SignRequest:
      properties:
        data:
          contentEncoding: base64
          type: string
        detached:
          type: boolean
        existingSignature:
          contentEncoding: base64
          type: string
        format:
          enum:
          - cms
          - xml
          - wsse
          type: string
        inputPem:
          type: boolean
        key:
          $ref: '#/components/schemas/KeySpec'
        noCheckCertTime:
          type: boolean
        nodeId:
          type: string
        outputPem:
          type: boolean
        parentNamespace:
          type: string
        parentNode:
          type: string
        tsaUrl:
          type: string
        withTimestamp:
          type: boolean
      required:
      - format
      - data
      - key
      type: object
    SignResponse:
      properties:
        cadesLevel:
          enum:
          - BES
          - T
          type: string
        format:
          enum:
          - cms
          - xml
          - wsse
          type: string
        libError:
          $ref: '#/components/schemas/LibError'
        signature:
          contentEncoding: base64
          type: string
        timestamp:
          $ref: '#/components/schemas/Timestamp'
      type: object
    Signer:
      properties:
        cadesLevel:
          enum:
          - BES
          - T
          type: string
        certificate:
          $ref: '#/components/schemas/Certificate'
        chain:
          items:
            $ref: '#/components/schemas/Certificate'
          type: array
        chainComplete:
          type: boolean
        chainSignaturesVerified:
          type: boolean
        claims:
          $ref: '#/components/schemas/Claims'
        signatureAlgorithm:
          type: string
        signingTime:
          format: date-time
          type: string
        timestamp:
          $ref: '#/components/schemas/Timestamp'
        trustAnchorFound:
          type: boolean
        valid:
          type: boolean
        verifyInfo:
          type: string
      required:
      - certificate
      - valid
      - chainComplete
      - trustAnchorFound
      - chainSignaturesVerified
      type: object
    Subject:
      properties:
        bin:
          type: string
        businessCategory:
          type: string
        commonName:
          type: string
        country:
          type: string
        dn:
          type: string
        domainComponent:
          type: string
        email:
          type: string
        gender:
          enum:
          - MALE
          - FEMALE
          - NONE
          type: string
        givenName:
          type: string
        iin:
          type: string
        lastName:
          type: string
        locality:
          type: string
        orgUnit:
          type: string
        organization:
          type: string
        state:
          type: string
      type: object
    Timestamp:
      properties:
        genTime:
          format: date-time
          type: string
        hash:
          contentEncoding: base64
          type: string
        hashAlgorithm:
          type: string
        policy:
          type: string
        serialNumber:
          type: string
        tsa:
          type: string
      type: object
    TokenKey:
      properties:
        alias:
          type: string
        pin:
          type: string
        storage:
          type: string
      required:
      - storage
      - pin
      type: object
    TrustedCert:
      properties:
        cert:
          contentEncoding: base64
          type: string
        intermediate:
          type: boolean
      required:
      - cert
      type: object
    ValidateRequest:
      properties:
        cert:
          contentEncoding: base64
          type: string
        crl:
          contentEncoding: base64
          type: string
        encoding:
          enum:
          - pem
          - der
          - base64
          type: string
        method:
          enum:
          - ocsp
          - crl
          type: string
        responderUrl:
          type: string
        trustedCerts:
          items:
            $ref: '#/components/schemas/TrustedCert'
          type: array
        wantOcsp:
          type: boolean
      required:
      - cert
      type: object
    ValidateResponse:
      properties:
        info:
          type: string
        libError:
          $ref: '#/components/schemas/LibError'
        ocspResponse:
          contentEncoding: base64
          type: string
        status:
          $ref: '#/components/schemas/RevocationStatus'
        warnings:
          items:
            $ref: '#/components/schemas/Warning'
          type: array
      required:
      - status
      type: object
    VerifyRequest:
      properties:
        checkCertTime:
          type: boolean
        claims:
          type: boolean
        data:
          contentEncoding: base64
          type: string
        detached:
          type: boolean
        extractContent:
          type: boolean
        format:
          enum:
          - cms
          - xml
          - wsse
          type: string
        inputPem:
          type: boolean
        signature:
          contentEncoding: base64
          type: string
        trustedCerts:
          items:
            $ref: '#/components/schemas/TrustedCert'
          type: array
      required:
      - format
      - signature
      type: object
    VerifyResponse:
      properties:
        content:
          contentEncoding: base64
          type: string
        detached:
          type: boolean
        format:
          enum:
          - cms
          - xml
          - wsse
          type: string
        libError:
          $ref: '#/components/schemas/LibError'
        signers:
          items:
            $ref: '#/components/schemas/Signer'
          type: array
        valid:
          type: boolean
        warnings:
          items:
            $ref: '#/components/schemas/Warning'
          type: array
      required:
      - valid
      - detached
      type: object
    Warning:
      properties:
        field:
          type: string
        reason:
          type: string
      required:
      - field
      - reason
      type: object
info:
  description: REST/JSON transport for the qoltanba digital-signature service (RK
    / Kalkan). One call handles one item. Binary fields are base64-encoded strings;
    times are RFC 3339. All request and response keys are lowerCamelCase. Generated
    from the Go types — do not edit by hand.
  license:
    name: Proprietary Kalkan library is BYOL; this service is separate.
  title: qoltanba REST API
  version: v1
openapi: 3.1.0
paths:
  /cert/info:
    post:
      operationId: certInfo
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CertInfoRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CertInfoResponse'
          description: Parsed certificate plus optional chain and claims
        "400":
          $ref: '#/components/responses/Error'
        "500":
          $ref: '#/components/responses/Error'
        "503":
          $ref: '#/components/responses/Error'
      summary: Fully parse a certificate (optionally build/validate the chain, add
        claims)
      tags:
      - certificate
  /cert/validate:
    post:
      operationId: certValidate
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ValidateRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidateResponse'
          description: Revocation-status outcome
        "400":
          $ref: '#/components/responses/Error'
        "500":
          $ref: '#/components/responses/Error'
        "503":
          $ref: '#/components/responses/Error'
      summary: Check a certificate's revocation status (OCSP / CRL)
      tags:
      - certificate
  /extract:
    post:
      operationId: extract
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExtractRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExtractResponse'
          description: Recovered content
        "400":
          $ref: '#/components/responses/Error'
        "500":
          $ref: '#/components/responses/Error'
        "503":
          $ref: '#/components/responses/Error'
      summary: Recover the original content from an attached signature
      tags:
      - signature
  /healthz:
    get:
      operationId: healthz
      responses:
        "200":
          description: ok
      summary: Liveness — the process is up
      tags:
      - observability
  /metrics:
    get:
      operationId: metrics
      responses:
        "200":
          description: Prometheus text exposition
      summary: Prometheus metrics
      tags:
      - observability
  /readyz:
    get:
      operationId: readyz
      responses:
        "200":
          description: ready
      summary: Readiness — library loaded and self-tested
      tags:
      - observability
  /sign:
    post:
      operationId: sign
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SignRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SignResponse'
          description: Signature produced (check libError for a crypto-core failure)
        "400":
          $ref: '#/components/responses/Error'
        "500":
          $ref: '#/components/responses/Error'
        "503":
          $ref: '#/components/responses/Error'
      summary: Sign data (CMS / XML / WSSE)
      tags:
      - signature
  /sign/add:
    post:
      operationId: signAdd
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SignRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SignResponse'
          description: Signature produced
        "400":
          $ref: '#/components/responses/Error'
        "500":
          $ref: '#/components/responses/Error'
        "503":
          $ref: '#/components/responses/Error'
      summary: Co-sign — add a signer to an already-signed container
      tags:
      - signature
  /statusz:
    get:
      operationId: statusz
      responses:
        "200":
          description: Status info
      summary: Non-sensitive service status (no secrets)
      tags:
      - observability
  /verify:
    post:
      operationId: verify
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VerifyRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VerifyResponse'
          description: Verification outcome (an invalid signature is valid=false +
            libError, still HTTP 200)
        "400":
          $ref: '#/components/responses/Error'
        "500":
          $ref: '#/components/responses/Error'
        "503":
          $ref: '#/components/responses/Error'
      summary: Verify a signature and extract everything available
      tags:
      - signature
servers:
- description: Default REST work port (-http :8080)
  url: http://localhost:8080
tags:
- description: Sign, verify, extract
  name: signature
- description: Certificate parsing and revocation
  name: certificate
- description: Health, status, metrics
  name: observability
